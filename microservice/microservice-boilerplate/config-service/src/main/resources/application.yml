server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: none
          clone-on-start: false
        native:
          search-locations: file:${user.dir}/config-repo
